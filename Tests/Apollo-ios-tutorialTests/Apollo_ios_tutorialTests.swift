import XCTest
@testable import Apollo_ios_tutorial

final class Apollo_ios_tutorialTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Apollo_ios_tutorial().text, "Hello, World!")
    }
}
